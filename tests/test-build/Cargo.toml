[package]
name = "test-build"
version = "1.0.0"
authors = ["Lucien XU <sfietkonstantin@free.fr>"]
edition = "2018"
build = "build.rs"

[dependencies]
qt-sys = {path = "../../qt-sys", features = ["qml"]}

[build-dependencies]
qt-binding-build = {path = "../../qt-binding-build", features = ["qml"]}
